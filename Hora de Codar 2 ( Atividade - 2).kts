fun main(){

//2. Faça um programa que leia um valor informado pelo usuário e diga se o valor informado é positivo, negativo ou zero.

            println(" O valor informado é:")
    var valor= readln().toDouble()
    if ( valor > 0){
        println("\n O valor  é positivo")
    }
    else if ( valor < 0){
        println("\n O valor é negativo")
    }
    else{
        println("\n O valor zero é neutro")
    }
}
}
}